# savings-goal-wallet

## Desarrollo

Instala las dependencias y arranca el proyecto desde la raíz:

```bash
yarn install
yarn dev
```

`yarn dev` pregunta si debe ejecutar Android o iOS, inicia el servidor web y
la app. Para Android también configura `adb reverse` para el puerto 5173.