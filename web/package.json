{
  "name": "savings-goal-web",
  "version": "1.0.0",
  "private": true,
  "description": "WebView micro-app — goal detail and deposit form",
  "scripts": {
    "dev": "npx serve . -p 3000",
    "sync": "node ../scripts/sync-web.js"
  }
}
